# Detecção (SCRFD) e reconhecimento (ArcFace 512-d). Substituíram o
# face_recognition/dlib, cujo embedding de 128-d não separava as pessoas
# da biblioteca de teste — ver docs/agrupamento_facial.md.
insightface
onnxruntime
numpy
scipy  # clusterização hierárquica (average linkage) em identity_store.py
pillow
pillow-heif
tqdm
